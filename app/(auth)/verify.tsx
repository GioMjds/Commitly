import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function VerifyScreen() {
    return (
        <SafeAreaView>
            <Text>Welcome to the Verify Screen!</Text>
        </SafeAreaView>
    )
}